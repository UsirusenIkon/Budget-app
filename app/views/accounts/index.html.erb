<p style="color: green"><%= notice %></p>

<h1>Accounts</h1>

<div id="accounts">
  <% @accounts.each do |account| %>
    <%= render account %>
    <p>
      <%= link_to "back to group", groups_path %>
    </p>
  <% end %>
</div>

<%= link_to "New account", new_group_account_path(:group_id) %>
